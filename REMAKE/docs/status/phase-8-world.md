# Phase 8 World Status

Audit override: the scoped World foundation remains an implementation baseline, but its integration closure is reopened. The landmark-instance defect and remaining expedition contracts are tracked by `REMAKE/docs/planning.md`; resource authority, encounter continuation, atomic death rollback, the fresh ending spine, and the parser parity graph are now remediated. The `RA-P1-15` Phase 9/12 breadth pause is lifted; new breadth must map to `DATA/parity-graph.json`.

The Phase 8 implementation baseline was recorded on 2026-07-09; the audit invalidated its integration-closure claim on the same date.

This is not a claim of full World/setpiece/executioner parity. Phase 8 owns original World generation, movement, visibility, travel survival, random encounters, landmark entry bridges, roads, Outpost use-state, mine safe-return consequences, and World-side Ship/Fabricator discovery consequences. Broader setpiece/outpost content remains Phase 9, player-facing Ship/Fabricator modules remain Phase 10/11, exhaustive Executioner content remains Phase 12, and full parity QA remains Phase 14.

Current baseline:

- Compass purchase ensures an original-shaped 61x61 World map exists.
- Terrain generation follows the original spiral/ring fill, terrain probabilities, and stickiness rules through deterministic engine RNG.
- Landmark placement uses original counts and radius bands, with a bounded fallback for degenerate deterministic RNG.
- Visibility uses the original diamond light mask and persists in `game.world.mask`.
- Scout `buy map` now uses the World runtime capability bridge to reveal a random hidden mask tile with the original radius-5 diamond uncover behavior, persists the mask, updates `game.world.seenAll` so the map button hides when fully revealed, and has browser coverage from an active World expedition through visible map reveal, original notification, and fully revealed button hiding.
- Safe village return now mirrors original `World.goHome()` by testing the persisted mask and setting `game.world.seenAll` after normal exploration reveals the last hidden tile.
- Ship direction is stored from generated ship placement for Compass messaging.
- Embark starts at the original village coordinate, renders the full original 61x61 World map through the persisted visibility mask with blank hidden tiles, current-position `@`, original tooltip metadata for the current wanderer, village, and visible unconsumed landmarks, and visited landmark glyph normalization, supports movement through buttons, pure-UI/browser-covered keyboard input, original map-click quadrants, and original swipe directions, emits original terrain-transition narration for forest/field/barrens movement into the visible World notification log with browser coverage for Forest to Field to Barrens movement, advances food/water and random-fight ticks only on original travel tiles instead of village/landmark tiles, auto-returns safely when movement enters the village tile, auto-enters unconsumed landmark tiles through the World setpiece bridge, and can return safely from the village.
- The visual 61x61 map is intentionally hidden from assistive technology. A separate compact World region provides the current named coordinates and terrain, resources, village distance/direction, allowed cardinal moves, and up to three nearest revealed landmarks with direction/distance; its visibility-filtered snapshot and Chromium accessibility tree coverage prevent hidden-tile leakage.
- Original mine-road drawing is ported and source-backed: when an active World expedition is standing on a cleared mine tile, the runtime draws the original L-shaped road to the closest village/road/outpost anchor and marks the mine tile visited without widening the ASCII grid glyph. Organic Iron/Coal/Sulphur Mine traversal is covered from World movement auto-entry through combat clear into the active road/visited consequence, generated-map Iron/Coal/Sulphur Mine reachability is covered from Compass map generation through normal World entry, road/visited consequences, safe village return, and safe-return building/worker consequences, and browser coverage now proves visible Mine tooltip labeling, movement-based Mine setpiece entry, and a real generated-map Iron Mine route from Compass generation through visible tooltip, mine entry, and original combat start.
- Original `clearDungeon` consequences are now applied for active cleared Cave/Town/City landmarks and the final cleared Battleship command route: the current tile becomes an Outpost and receives an original road connection. Cave, Town, City, and final Battleship conversion now have organic or active World traversal coverage into immediate converted-Outpost water refill, coordinate-specific used-state, hidden landmark state, and repeat-entry blocking.
- Original visited-marker consequences are now applied for one-off landmarks such as Old House, Swamp, Borehole, Battlefield, Destroyed Village cache, and Crashed Ship; Borehole has organic Alien Alloy salvage coverage plus browser coverage for visible label, movement-based entry, loot transfer, and used-entry hiding, Battlefield has organic equipment salvage coverage plus browser coverage for visible label, movement-based entry, deterministic loot pickup, and used-entry hiding, Swamp has organic Charm-gated `gastronome` coverage plus browser coverage for visible label, movement-based wanderer talk, used-entry hiding, safe return, and visible perk display, Old House has organic supplies-branch loot and water-refill coverage plus browser coverage for visible label, movement-based occupied-branch entry, combat completion, and used-entry hiding, Destroyed Village has organic cache collection/prestige-store transfer coverage plus browser coverage for visible label, movement-based entry, prestige-store transfer, `previous.stores` clearing, and used-entry hiding, and Crashed Ship draws its original road when discovered with organic and browser coverage for visible label, movement-based salvage, used-entry hiding, and road drawing.
- Original Executioner entry is now covered from organic World movement onto a Ravaged Battleship tile through the focused first-visit ancient-beast/turret intro chain, device discovery, safe return, Fabricator unlock notification, return-visit antechamber routing into Engineering/Medical/Martial wing entry events, organic Engineering Assembly/R&D clear into the `game.world.engineering` flag, organic Medical guardians/strategy/cold-storage clear into the `game.world.medical` flag, organic Martial armory/training-complex clear into the `game.world.martial` flag, focused Command Deck clear, and active Battleship-to-Outpost conversion; browser coverage now proves visible Ravaged Battleship labeling, movement-based first-visit Executioner event entry, movement-based return-visit antechamber entry, visible wing choices, Command Deck gating before and after wing completion, Medical Wing entry, and Command Deck entry from the antechamber.
- Active setpiece water replenishment flags now refill World water to the current max and are consumed through the session update boundary.
- Safe village return now commits original mine-discovery building unlocks for iron, coal, and sulphur mines, allowing Outside worker unlock state to follow the original `World.goHome()` contract. Organic Iron/Coal/Sulphur Mine clearing is covered through road travel back to the village and safe-return worker unlock state, and browser coverage now proves a movement-entered Coal Mine clear through village safe return into visible `coal miner` unlock.
- Active Outpost use now replenishes World water to the current max, exposes original Cured Meat scene loot through the organic World auto-entry path, stores coordinate-specific used-outpost state for the current expedition, hides that Outpost from further landmark entry while preserving the visible map glyph, resets used Outposts on the next embark after a safe return, and has browser coverage for movement-based Outpost entry, visible water refill, scene loot, used-Outpost entry hiding, preserved non-interactive `P` map glyph, safe return, re-embark, restored Outpost tooltip/entry, and same-Outpost reuse after the expedition reset.
- World food and water movement ticks now follow the original `slow metabolism` and `desert rat` cadence, Cured Meat heals with the `gastronome` bonus, and repeated starvation/dehydration returns the player to the room while incrementing original death counters and awarding the corresponding survival perk on the tenth death. Every World, Combat, or lethal event-cost death now restores the embark-time World draft, destroys the carried outfit, closes World, and starts the visible 120-second Path embark cooldown; fresh-run browser coverage proves Old-House mutation rollback, Room return, World-tab closure, exact cooldown boundaries, re-embark, and landmark restoration.
- World random fights now follow original `FIGHT_DELAY`, `FIGHT_CHANCE`, and `stealthy` behavior before routing through the existing terrain/distance encounter bridge, with browser coverage proving movement waits through the original delay, starts a World-selected encounter after the delay, and resets the fight-move counter.
- Won wilderness encounters now close only the event: active World position, transaction draft, HP, water, carried supplies, and taken loot remain intact, and fresh-run Chromium coverage proves another movement after loot and leave.
- World danger state now follows the original distance/armour thresholds, emits the original danger/safer notifications when crossing those thresholds into the visible World notification log, exposes active danger/starvation/thirst conditions in the World status panel, and has browser coverage for movement-driven danger/safer visible status plus notification transitions.
- Safe village return now commits original Ship discovery by unlocking `features.location.spaceShip` and initializing base hull/thrusters, and commits Fabricator discovery from the Executioner flag with the original builder notification. Phase 8 owns only these World-side discovery consequences; player-facing Ship and Fabricator modules remain Phase 10/11 work.

Moved out of Phase 8 scope:

- Broader Outpost/setpiece polish beyond browser-covered World entry, supplies, per-expedition active use, preserved used-Outpost glyph/hidden tooltip behavior, converted dungeon/Battleship use-state coverage, and re-entry blocking belongs to Phase 9 Setpieces and Dungeons.
- Broader generated-map/player-facing interaction QA beyond the covered active-clear paths, generated Iron/Coal/Sulphur Mine routes, browser-covered generated Iron Mine route, visible Mine/Borehole/Battlefield/Swamp/Old House/Destroyed Village/Crashed Ship label-to-entry browser coverage, browser-covered Coal Mine safe-return worker unlock, Scout map reveal/hiding, full map rendering, tooltips, keyboard movement, map-click movement, swipe movement, and browser-covered random-fight cadence belongs to Phase 14 full parity QA unless a concrete regression appears earlier.
- Exhaustive Executioner traversal beyond browser-covered first-visit/return-visit/Command-Deck entry, focused organic intro, return-visit antechamber wing entries, organic Engineering/Medical/Martial clears, and Command Deck clear paths belongs to Phase 12 Executioner Content.
- Full original world UI interaction parity remains a Phase 14 QA target, using the Phase 8 World foundation as the baseline.

Guardrails:

- Preserve the finalized Phase 8 World foundation while entering Phase 9.
- Do not pull player-facing Ship/Fabricator module UI into Phase 8; those remain Phase 10/11.
- Treat broad setpiece and exhaustive Executioner gaps as later-phase scope unless a concrete Phase 8 regression is found.
