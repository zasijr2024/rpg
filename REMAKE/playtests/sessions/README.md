# Session Records

The version-2 schema, validator, tests, and `../session.example.json` are ready. Final P14V-06 collection begins only after P14V-05 freezes the exact candidate revision, ruleset, and production artifact.

When unblocked, place one consented, de-identified JSON file per qualifying session here. Every file in a release-decision cohort must use the same exact revision, production artifact, and ruleset; IDs must be unique. Follow `../README.md`, validate with `npm run study:human`, and close the preliminary three-session minimum with `npm run study:human:gate`.

Do not place names, contact details, recordings, debug/coached runs, automated sessions, or copied/duplicate records here. The summarizer ignores this README.
